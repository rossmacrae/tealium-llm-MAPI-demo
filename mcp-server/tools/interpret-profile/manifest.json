{
  "name": "interpret-profile",
  "description": "Summarizes a customer profile into a brief human-readable insight.",
  "parameters": {
    "type": "object",
    "properties": {
      "profile": {
        "type": "object",
        "description": "The raw profile data from Tealium, including properties, metrics, and flags."
      }
    },
    "required": ["profile"]
  },
  "returns": {
    "type": "object",
    "properties": {
      "summary": {
        "type": "string",
        "description": "A natural language summary of the customer's situation, including churn risk, products held, tenure, etc."
      }
    },
    "required": ["summary"]
  }
}

