version: 1.0
runtime: nodejs22
build:
  commands:
    - npm ci
    # If you compile TypeScript or bundle, keep this; otherwise remove:
    # - npm run build
run:
  # If you run from dist, change to: node dist/server.js
  command: node server.js
  network:
    port: 3002

